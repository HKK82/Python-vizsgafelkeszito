name: Temporary exam logging validation

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/tmp-exam-log-validate.yml'

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: JavaScript syntax
        run: |
          node --check js/firebase-service.js
          node --check js/exam-app.js
          node --check js/teacher.js
      - name: Full validator
        run: node tools/validate.mjs
